=== 107
(a)
vector<int> vec;
list<int> lst;
int i;
while (cin >> i)
	lst.push_back(i);
//copy(lst.cbegin(), lst.cend(), vec.begin());
copy(lst.cbegin(), lst.cend(), back_inserter(vec));

(b)
vector<int> vec;
vec.reserve(10);
//fill_n(vec.begin(), 10, 0);
fill_n(back_inserter(vec), 10, 0);

=== 108
因为 back_inserter 使用的是 push_back 操作，操作结束后，
迭代器也更新了。
