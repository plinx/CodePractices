#include <iostream>
#include <string>

#include "OOP.h"


int main()
{
	_int i(0);
	_string s("0");

	/*auto ii = i.add(1).eval();
	auto is = i.add("s").eval();
	auto si = s.add(0).eval();
	auto ss = s.add("1").eval();

	std::cout << ii.getint() << " " << is.getint() << " " << si.getstring() << " " << ss.getstring() << std::endl;
	*/

	return 0;
}